# Update report — ProjectCard / HOW IT WORKS visual polish — 2026-07-25

Builds on `UPDATE_REPORT_2026-07-25_BASELINE.md`. Scope: visual/animation polish only, no behavior change beyond one timing constant.

## Request summary

Polish the left-column button hierarchy, workflow row active-state clarity, mini-node reveal, and autoplay dwell timing — without touching Hero/Plasma/Nav logic/Archive/Footer/deployment/data, and without reintroducing any of the systems listed as "do not reintroduce" in the baseline report.

## Files changed this pass

- `src/styles/global.css` — five scoped rule changes (below).
- `src/hooks/useWorkflowWalkthrough.ts` — `STEP_DWELL_MS` 1400 → 1800.

## Exact diff (this pass's rules)

```diff
 .mini-roadmap{
-  margin-top:16px;color:var(--ink-2);font-size:10px;line-height:1.8;
+  margin-top:18px;color:var(--ink-2);font-size:10px;line-height:1.8;
   max-width:280px;
 }
 .mini-roadmap-arrow{color:var(--ink-3)}
-.proof-chips{display:flex;flex-wrap:wrap;gap:8px;margin-top:16px}
+.proof-chips{display:flex;flex-wrap:wrap;gap:8px;margin-top:18px}
```

```diff
-.p-controls{display:flex;flex-direction:column;align-items:flex-start;gap:10px;margin-top:24px}
-.p-controls .view-details{margin-top:0}
+.p-controls{display:flex;flex-direction:column;align-items:flex-start;gap:12px;margin-top:28px}
+.p-controls .view-details{
+  margin-top:0;border:none;background:none;padding:0;
+  color:var(--ink-3);text-decoration:underline;text-decoration-color:transparent;
+  transition:color .15s ease,text-decoration-color .15s ease;
+}
+.p-controls .view-details:hover{background:none;color:var(--ink);text-decoration-color:var(--ink-3)}
```
(Scoped to `.p-controls .view-details` specifically — the base `.view-details` class used by Archive cards is untouched, verified live below.)

```diff
+.w-step--active{
+  background:rgba(30,107,78,.03);
+  margin:0 -12px;padding-left:12px;padding-right:12px;
+  box-shadow:inset 3px 0 0 var(--accent);
+}
 .w-step--active .w-step-title{color:var(--ink)}
```
(Same inset-accent technique `.system-card` already uses — flat color bar, no drop shadow, matching existing visual language.)

```diff
 .w-mini-node{
   ...
   transition:opacity .3s ease, transform .3s ease;
 }
+.w-mini-node:nth-child(2){transition-delay:.08s}
+.w-mini-node:nth-child(3){transition-delay:.16s}
+.w-mini-node:nth-child(4){transition-delay:.24s}
 .w-mini-node--in{opacity:1;transform:none}
 @media (prefers-reduced-motion:reduce){
-  .w-mini-node{transition:none;opacity:1;transform:none}
+  .w-mini-node{transition:none;transition-delay:0s !important;opacity:1;transform:none}
 }
```
(CSS-only stagger, no JS timing added.)

```diff
-const STEP_DWELL_MS = 1400
+const STEP_DWELL_MS = 1800
```

## Build result

```
> ai-projects-showcase@0.1.0 build
> tsc -b && vite build

vite v5.4.21 building for production...
✓ 112 modules transformed.
dist/index.html                 0.82 kB │ gzip:  0.45 kB
dist/assets/index-D_pfi2SZ.css  15.72 kB │ gzip:  3.62 kB
dist/assets/index-DBQPy3Wk.js   230.84 kB │ gzip: 72.32 kB
✓ built in 552ms
```
Zero errors.

## Verification (Puppeteer reconnected this session — actually tested, not just traced)

- **Archive button untouched:** `#method-of-loci`'s `.view-details` computed style — border `0.8px solid rgb(30,107,78)`, unchanged pill. Confirms the scoped selector didn't leak.
- **Project 01's "View details" now secondary:** border `none`, color `rgb(143,140,131)` (muted `--ink-3`), transparent background — reads as quiet text-link next to the filled `.how-it-works-btn` (background `rgb(30,107,78)`).
- **Active row accent:** computed `box-shadow: rgb(30,107,78) 3px 0px 0px 0px inset`, background `rgba(30,107,78,0.03)` — confirmed present on the active `.w-step`.
- **Mini-node stagger:** computed `transition-delay` read back as `0s`, `0.08s`, `0.16s` for the 3 chips in Project 01's step 1 — matches the CSS.
- **Full autoplay sequence at the new 1800ms dwell**, tested on Project 04 (`evidence-review-decision-gate`): step 1 "Structured extraction" → step 2 "Evidence and consistency check" → step 3 "Independent phrase scan" → step 4 "Debug memory update" → recap takeaway "Mistakes become reusable review rules." — all landed at the expected ~1800ms intervals.
- Also re-confirmed from the baseline (still holds after this pass): SYSTEMS nav → `#systems` lands correctly; all 7 project cards collapsed on a clean URL; Projects 01–04 all have `.how-it-works-btn` + `.walkthrough`; Projects 05–07 have neither.

## Manual test checklist (for you to also confirm visually — computed styles were checked, not pixels)

- Hard-refresh `localhost:5191`.
- Look at Project 01's left column: does "How it works" read as clearly primary and "View details" as clearly secondary/quiet now?
- Click "How it works" on any of Projects 01–04: does the active row show a left accent bar + faint tint, and do the mini-node chips fade in with a slight stagger rather than all at once?
- Does the ~1.8s dwell per step feel readable, not slow?
- Confirm Archive cards (Method of Loci, Video Pipeline, Blender+ComfyUI) still look exactly as before — same button style, no left-accent row (they don't use `.w-step` at all).

## Preservation check

- Hero touched? No.
- Plasma touched? No.
- Nav touched? No (this pass — nav.href change was the prior pass).
- ProjectCard touched? No (only `global.css` and the dwell constant).
- Archive touched? No — verified live, computed styles unchanged.
- Footer touched? No.
- Deployment touched? No.
- Data touched? No.

## Remaining risks / not yet confirmed

- Everything above was checked via computed CSS values and DOM state through Puppeteer, not a human eye on actual rendered pixels — recommend a visual pass to confirm the accent bar and stagger actually *look* good, not just that the values are technically present.
- `docs/` has not been rebuilt and nothing has been committed yet — still sitting as uncommitted working-tree changes, same as the whole session so far.
